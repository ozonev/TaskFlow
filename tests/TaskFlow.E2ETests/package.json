{
  "name": "@taskflow/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Playwright E2E tests driving the real TaskFlow API + Web dev servers over HTTP against an isolated SQLite database.",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^22.15.0",
    "typescript": "^5.8.3"
  }
}
